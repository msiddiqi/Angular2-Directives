import { RotatedDirective } from './rotated.directive';

describe('RotatedDirective', () => {
  it('should create an instance', () => {
    const directive = new RotatedDirective();
    expect(directive).toBeTruthy();
  });
});
